<!-- <div class="l-container">
  <header class="l-site-head">
    <a href="/" class="invert-link">
      <span class="l-site-name">
        {% include "partials/icons/logo.svg" %}
        <span class="l-site-title site-title">{{ site.name }}</span>
      </span>
    </a>
    {% set navLocation = 'primary' %}
    {% include "partials/nav.njk" %}
  </header>
</div> -->

<!-- <a class="[ skip-link ] [ button ]" href="#main-content">Skip to content</a> -->
<header role="banner" class="site-head">
  <div class="wrapper">
    <div class="site-head__inner">
      <a href="/" aria-label="{{ site.name }} - home" class="site-head__brand">
        <img src="/outside-logo.png" alt="outside-logo" />
      </a>
      <nav
        class="[ nav ] [ site-head__nav ]"
        aria-label="Primary navigaton"
      >
        <ul class="nav__list">
          {% for item in navigation.items %}
          <li class="nav__list-item">
            <a href="{{ item.url }}" {{ helpers.getLinkActiveState(item.url, page.url) | safe }}>
              {{ item.text }}
            </a>
          </li>
          {% endfor %}
        </ul>
      </nav>
    </div>
  </div>
</header>